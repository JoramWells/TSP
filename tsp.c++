#include <bits/stdc++.h>
using namespace std;
#define V 4

int travellingSalesman(int graph[][V],int s){
    vector<int> vertex;
    for(int i=0;i<V;i++)
        if(i!=s)
            vertex.push_back(i);
    int min_path = INTMAX_MAX;
    do
    {
        int current_pathweight = 0;
    } while (next_permutation(vertex.begin(),vertex.end()));
    
}